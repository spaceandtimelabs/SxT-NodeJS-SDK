var credentials;
// Set credentials
export const SetCredentials = (inCredentials) => {
    credentials = inCredentials;
};
// Retrieve credentials
export const GetCredentials = () => {
    return credentials;
};
